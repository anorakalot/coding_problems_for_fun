with Ada.Text_IO;
with Ada.Strings.Bounded;
procedure Lab_5_Book is
   type company_record is record
      --first_name : Ada.Strings.Bounded.Bounded_String(1..50);
      --first_name : String(1..50);
      first_name : Bounded_string;
      --last_name : String(1..50);
      end record;
begin
    --  Insert code here.

   null;
end Lab_5_Book;
