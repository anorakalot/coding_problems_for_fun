class Solution {
public:
    vector<int> partitionLabels(string s) {
        //ok so there's the bad solution of going through it multiple times but I don't know if that's the way I want to go.
        //like for each unique value keep track of where it is and see how far it goes
    }
};