/**
 * Note: The returned array must be malloced, assume caller calls free().
 */
char** summaryRanges(int* nums, int numsSize, int* returnSize) {
    
    //go through the array for loop
    //if the array is >0 and the array index is


    //ok so the biggest thing is checking either after the current index or before the current index;
    //checking after erases needing to do a skip i = 0 method
    myString = (char *)malloc(sizeof(char) * (3 + 1));

}