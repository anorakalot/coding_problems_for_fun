class Solution
{
public:
	vector<string> restoreIpAddresses(string s)
	{
		// if (s == )//base case here

		vector<string> return_val;

		return_val = restore_ip_addr_recur(s, 0);
	}
	vector<string> restore_ip_addr_recur(string s, int index)
	{
	}
};